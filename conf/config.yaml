defaults:
  - model: default
  - dataset: default

training:
  batch_size: 64
  micro_batch_size: 8
  learning_rate: 1e-3
  num_steps: 10
  use_checkpointing: true
  use_mixed_precision: true
  use_zero: true
  num_workers: 2
  seed: 0
